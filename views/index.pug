html
  title CYPhil_Homepage
  head
    style
    link(type="text/css", href='css/styling.css', rel='stylesheet')
    link(type="text/css", href='css/materialize.css', media="screen,projection") 
    link(link href="https://fonts.googleapis.com/css?family=VT323" rel="stylesheet")
    link(href="https://fonts.googleapis.com/css?family=Economica" rel="stylesheet")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")

 

block content      
body
nav
  .nav-wrapper
    .ul
      .li
        a.a(href='/register') Register
      .li
        a.a(href='/userlogin') Login
      .li
        a.a(href='/about') About


    p 
    img(src='images/selfdestr.jpg', alt='LOGO', height='142', width='110', align='middle') 

    .h1 Cyber-Poetry CAN save your life. Register to get the most awesome poetry 

    script(type='text/javascript', src='/js/jquery-3.1.1.min.js')
    script(type='text/javascript', src='/js/js.cookie.js')
    script.
      var hasVisited = Cookies.get('visited')
      if(hasVisited === 'true'){
        $(document).ready(function(){
          $('h1').html('Cyber-Poetry will save your soul. You can log in to see your lifesaving collection')
        })
      }
      else {
        Cookies.set('visited', 'true', {expires:7})
      }



